import { Events } from 'discord.js';
//const { Logger } = await import(process.env.src + 'function/logger.js');

export const name = Events.StageInstanceUpdate;
export async function execute(/*oldState, newState, client*/) {
	//const log = new Logger(this.name);
	//log.info(oldState);
	//log.info(newState);
}